# gateway-auth-service

Internal OTLP token validation and gateway auth service used by BeObservant.

## Tests

```bash
python -m pytest -q
```

If you are committing from the BeObservant repository root, `.pre-commit-config.yaml`
executes this test suite automatically along with other service and UI checks.
